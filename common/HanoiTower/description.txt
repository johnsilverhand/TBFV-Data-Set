//汉诺塔
//修改下述
T1 := n <= 0 ∨ N <= 0; D1 := s = "none"   :当盘子数和最大步数出现异常时，输出 "none"
T2 := n = 1 ∧ N >= 1; D2 := s = 1         :只有一个盘子，只需要1步
T3 := n > 1 ∧ N > 1; D3 := (pow(2,n) - 1 <= N → s = pow(2,n) - 1) ∨ (pow(2,n) - 1 > N → s = 0): 正常求解最小步数与给定最大步数N比较
