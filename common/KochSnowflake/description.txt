//科赫雪花曲线

T1 := l <= 0 ∨ L <= 0 ∨ l > L; D1 := n = 0   :当等边三角形边长和指定边长异常时，输出最小迭代次数为0
T2 := l > 0 ∧ L > 0 ∧ l <= L; D2 := (pow((4/3),n) * l >= L ∧ pow((4/3),n-1) * l < L)         :正常计算输出最小迭代次数 n-1项不满足即为至少迭代次数
